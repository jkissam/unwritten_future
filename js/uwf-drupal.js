/**
 *  uwf-drupal.js
 *  Javascript specific to the implementation of Unwritten Future as a Drupal theme
 */
 
 // load theme options and translations into the variables that uwfutil.js uses
 uwfOptions = Drupal.settings.themeOptions;
 uwfText = Drupal.settings.themeTranslations;

/**
 *  jQuery(document).ready
 */
jQuery(document).ready(function($){
	
});

/**
 *  jQuery(window).load
 */
jQuery(window).load(function(){
	
});

/**
 *  jQuery(document).ajaxComplete
 */
jQuery(document).ajaxComplete(function() {
	
});

/**
 *  jQuery(window).smartresize()
 */
jQuery(window).smartresize(function(){
	
});